mixin card(titulo,precio,imagen)
	article.articulos__container
		img(src="./img/small/"+imagen, alt=""+titulo)
		.detalles 
			p=titulo
			h1=precio

mixin card-categoria(titulo,icon,alt,link)
	
	.categorias__card
		a(href=""+link ) 
			img(src="./icons/"+icon, alt=""+alt)
		p=titulo
				